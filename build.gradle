apply plugin: 'java'

sourceSets {
	main {
		java.srcDirs = ['src']
		resources.srcDirs = ['src']
	}
}

jar {
	manifest.attributes('Main-Class': 'reo7sp.mclauncher.Core')
	from {
		configurations.compile.collect {
			it.isDirectory() ? it : zipTree(it)
		}
	}
}
